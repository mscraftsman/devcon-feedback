-- generated by gocipe; DO NOT EDIT

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

DROP TABLE IF EXISTS feedbacks;

CREATE TABLE feedbacks (
	"id" TEXT NOT NULL DEFAULT uuid_nil(),
	"status" TEXT NOT NULL DEFAULT 'draft',
	"user_id" TEXT NOT NULL DEFAULT uuid_nil(),
	"created_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	"session_id" TEXT NOT NULL DEFAULT '',
	"reaction1" TEXT NOT NULL DEFAULT '',
	"reaction2" TEXT NOT NULL DEFAULT '',
	"reaction3" TEXT NOT NULL DEFAULT '',
	"reaction4" TEXT NOT NULL DEFAULT '',
	"visitor_id" TEXT NOT NULL DEFAULT uuid_nil(),
	PRIMARY KEY ("id")
);

DROP TABLE IF EXISTS visitors;

CREATE TABLE visitors (
	"id" TEXT NOT NULL DEFAULT uuid_nil(),
	"status" TEXT NOT NULL DEFAULT 'draft',
	"user_id" TEXT NOT NULL DEFAULT uuid_nil(),
	"created_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	"name" TEXT NOT NULL DEFAULT '',
	"bookmark" TEXT NOT NULL DEFAULT '',
	"photo_link" TEXT NOT NULL DEFAULT '',
	PRIMARY KEY ("id")
);

DROP TABLE IF EXISTS ratings;

CREATE TABLE ratings (
	"id" TEXT NOT NULL DEFAULT uuid_nil(),
	"status" TEXT NOT NULL DEFAULT 'draft',
	"user_id" TEXT NOT NULL DEFAULT uuid_nil(),
	"created_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	"updated_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	"rating_count" TEXT NOT NULL DEFAULT '',
	"score" INTEGER NOT NULL DEFAULT 0,
	"reaction_summary" TEXT NOT NULL DEFAULT '',
	PRIMARY KEY ("id")
);

